//linera searrch
#include <stdio.h>
int main(){
    int s;
    scanf("%d",&s);
    int size;
    scanf("%d",&size);
    int arr[size];
    for(int i=0;i<=size;i++){
        scanf("%d",arr[size]);
    }printf("%d",&arr[size]);
    printf("%d",&size);
}