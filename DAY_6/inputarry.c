#include <stdio.h>
int main(){
     int size;
    scanf("%d",&size)
    int arr[]={1,2,3,4,5};
    for( int i=0;i<size;i++){
        scanf("%d",&arr[i]);
    }
    for(int i=0;i<size;i++){
        printf("%d",&arr[size]);
    }
}